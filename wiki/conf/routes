# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                                                                       Page.Index

# Ignore favicon requests
GET     /favicon.ico                                                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                                                       Static.ServeModule(wiki,public)

# List
GET     /list                                                                   List.Index
GET     /list/:order                                                            List.Index

# Page
GET     /:pageName                                                              Page.Show
GET     /:pageName/modify                                                       Page.Modify
POST    /:pageName/modify                                                       Page.Save
GET     /:pageName/revisions                                                    Page.Revisions
GET     /:pageName/diff/:revisionId                                             Page.Diff
